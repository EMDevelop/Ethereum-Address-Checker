class DirectTransactions

  def initialize(transactions,adresses)
    @all_addresses = transactions
    @origin_addresses = adresses
    @direct_transactions = {}
  end

  #Pass in the transactions and the addresses
  # use the same style as the hash we're operating on, but make a new hash with only matching transactions. 
  
  def analyse_addresses

  end

end